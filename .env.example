# Copy to .env and fill in. Required for the prompted humanizer.
OPENAI_API_KEY=sk-...

# Optional: point at a self-hosted OpenAI-compatible server (Ollama, vLLM, LM Studio).
# OPENAI_BASE_URL=http://localhost:11434/v1

# Optional: experiment tracking
# WANDB_API_KEY=...
# WANDB_PROJECT=humanizer
